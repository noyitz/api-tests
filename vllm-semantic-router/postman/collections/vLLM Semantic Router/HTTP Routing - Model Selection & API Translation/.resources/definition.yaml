$kind: collection
description: Tests for /v1/route with request mutations (body + header
  translation). Model-A is OpenAI format, Model-B is Anthropic format. Includes
  response translation via /v1/route/translate-response.
order: 8000
