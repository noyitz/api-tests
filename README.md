# API Tests

Postman collections for API testing.

## Structure

Each folder contains a Postman collection and environment file for a specific service.

## Usage

1. Import the collection JSON into Postman
2. Import the matching environment JSON
3. Update the environment variables with your actual URLs
4. Run the requests
